apiVersion: batch/v1
kind: Job
metadata:
  name: ai-trainer
  namespace: default
spec:
  ttlSecondsAfterFinished: 360
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: trainer
          image: esadik/ai-trainer:latest
          volumeMounts:
            - mountPath: /data
              name: training-data
      volumes:
        - name: training-data
          persistentVolumeClaim:
            claimName: ai-data-pvc
